export interface galleryImgProps {
  path: string;
  size: number;
  alt?: string;
  description?: string;
  local?: string;
  pessoas?: string;
  link?: string;
  author?: string;
  date?: string;
  type?: string // Se for vídeo ou foto
}
